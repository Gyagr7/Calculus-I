# Pin the PreTeXt-CLI version this project was set up and tested against.
# Run `pip install -r requirements.txt` (ideally in a virtual environment)
# before running any `pretext` commands.
pretext == 2.49.1
lxml==6.1.2  # 6.1.3 breaks PreTeXt XSL entity loading (2026-09-11)
